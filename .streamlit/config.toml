# Tema mengikuti pilihan pengguna (menu ☰ → Settings → Theme: Light/Dark/System).
# CSS aplikasi adaptif via variabel tema, jadi terang & gelap sama-sama rapi.
[theme]
primaryColor = "#1f9d55"

[server]
headless = true
